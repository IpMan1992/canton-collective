# Canton Collective Project

Operating harness for **Canton Collective** (cantoncollective.com) — CC only. HAE is a separate project.
Gen-Z, multi-brand Asian indie curation. Built fresh — patterns reused from HAE, nothing copied across.

## Built now (urgent)
- `store-manager` — weekly performance, vendor-share first, in the proven Google Sheet format.
- `trend` — per-sub-brand radar, Gen-Z Asian indie signals.

## To run (from inside this project, CC Shopify connected + verified)
- "Run the store manager — weekly performance."
- "Run the trend agent — baseline run." (first time; then bi-weekly)

## Setup
- Move the global `shopify-weekly-performance` skill is fine as-is (store-agnostic); CC's trend method
  lives in `agents/trend.md` (extract to a `cc-trend-radar` skill later if you want it reusable).
- `git init` here to version it and verify the firewall stays clean.

## Open items (search `TODO`/`Confirm`)
- MAMC / Outtheblue / UMAMIISM aesthetics; whether UMAMIISM is still active.
- Meta SGD account resolution. · Coordinator + other agents (planned).
